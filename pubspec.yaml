name: vegetables_shop
description: A new Flutter project for a vegetable shop app.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/Background.png
    - assets/Bawang.png
    - assets/Tomat.png
    - assets/Brokoli.jpg
    - assets/promo.png
    - assets/Home.png
    - assets/Ticket.png
    - assets/Message.png
    - assets/Profile.png
    - assets/keranjang.png